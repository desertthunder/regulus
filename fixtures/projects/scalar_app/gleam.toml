name = "scalar_app"
version = "1.0.0"
description = "A small project fixture for the Gleam to WASM compiler."
licences = ["Apache-2.0"]
target = "javascript"

[dependencies]
gleam_stdlib = ">= 0.44.0 and < 2.0.0"
